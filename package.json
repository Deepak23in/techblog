{
  "name": "techblog",
  "version": "1.0.0",
  "description": "Static technology blog site",
  "main": "index.js",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "serve": "npx @11ty/eleventy --serve",
    "debug": "DEBUG=* npx @11ty/eleventy",
    "publish": "npx gh-pages -d _site"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.10.0",
    "gh-pages": "^2.2.0"
  }
}
