---
title: Drones
tagName: drones
override:tags: 
    - category
---

{% for post in collections.drones %}
    <h2><a href="{{ post.url  | url }}">{{ post.data.title }}</a></h2>
    <em>{{ post.date | date: "%Y-%m-%d" }}</em>
{% endfor %}